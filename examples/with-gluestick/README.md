# redux-promise-memo example with GlueStick

This example project shows the setup using `redux-promise-memo` with [GlueStick](https://github.com/TrueCar/gluestick)

## Usage

```
npm install -g gluestick-cli
```

```
cd with-gluestick
npm install
gluestick start
```
